{
    "name": "inject-jyutping",
    "version": "0.5.0",
    "description": "A user script that adds Cantonese pronunciation (Jyutping) on Chinese characters",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "bun scripts/build.ts",
        "lint": "tsc -p tsconfig.json"
    },
    "devDependencies": {
        "@types/bun": "^1.1.10",
        "to-jyutping": "3.1.1",
        "typescript": "^5.6.2"
    }
}
